# Аудит SEO и видимости в нейросетях: mute.ac, сентябрь 2026

Дата аудита: 14 сентября 2026. Проверялся живой сайт https://mute.ac (ответы
сервера, разметка, robots, sitemap, llms.txt, выдача Google и Bing, Wikidata,
Lighthouse), код в этом репозитории и внутренние документы. Аудит мая 2026
выполнен целиком и удалён; ниже только текущее состояние и план на осень.

Правила формулировок для всех текстов сайта и внешних площадок: регулятор
упоминается только на юридических страницах; «без VPN» — единственная
допустимая связка с этим словом, и только про Mute; никаких советов и намёков,
как вернуть доступ к заблокированным сервисам. Проверка `npm run check:content`
запускается перед каждой сборкой и останавливает её при нарушении.

## 1. Итог

Техническая база у сайта сильная: SSR, свои title и description на каждой
странице, canonical, граф schema.org с `@id`, sitemap с честными датами,
robots с Clean-param для Яндекса, `llms.txt` и `llms-full.txt` на двух языках,
генерируемые OG-картинки, IndexNow, заголовки безопасности, кэширование
статики. Bing держит в индексе около 86 адресов, Google уже показывает главную
и статью про аналоги Discord в первых результатах по «аналог дискорда в россии
без vpn» и «голосовой чат для игр без vpn», элемент в Wikidata создан.
Lighthouse: SEO 100 на всех проверенных страницах, производительность главной
97 на мобильном и 100 на десктопе.

Главный риск на 14 сентября был не в технике, а в формулировках: около 70
строк на 20 страницах всё ещё описывали, как и через что открывается Discord.
Этот проход снимает их полностью и ставит автоматическую проверку.

Главные пробелы, которые остаются: нет данных из Google Search Console и Bing
Webmaster (оба не подключены), нет таблицы позиций и журнала проверок в
нейросетях, очередь внешних площадок стоит (опубликована одна статья из
девяти запланированных), у половины гайдов и статей title длиннее 60–70
символов, страница «Скачать» почти пустая при приоритете 0.9 в sitemap,
у гайдов на мобильном LCP около 5 секунд из-за иллюстрации.

## 2. Что уже хорошо, не трогать

- Рендеринг на сервере, `lang="ru"`, canonical на каждой странице, единый
  шаблон title «… — Mute», описания на ключевых посадочных до 160 символов.
- Граф schema.org в `lib/schema.ts`: Organization (с alternateName «Мьют»,
  «Мут», «Муте» и sameAs), WebSite, SoftwareApplication (цена 0, платформы,
  featureList, скриншоты), FAQPage только на главной, BreadcrumbList везде,
  Article и BlogPosting с dateModified, TechArticle на релизах и инструкциях.
- Sitemap на 41 адрес с датами из frontmatter, а не «сегодня»; robots.txt с
  блоком Yandex и Clean-param; `llms.txt` и `llms-full.txt` с фактами,
  ограничениями продукта и английским резюме.
- OG-картинки генерируются на лету для блога, гайдов и релизов.
- HSTS, nosniff, Referrer-Policy, Permissions-Policy, gzip, `immutable` для
  `_next/static`, 301 с http и www, 308 для хвостовых слэшей, честный 404.
- `beta.mute.ac` отдаёт `X-Robots-Tag: noindex, nofollow`.
- Перелинковка: хлебные крошки, RelatedLinks по frontmatter, CTA-баннер в
  гайдах и статьях, SeoIntro на главной.
- Метрика с целями, Top.Mail.Ru, IndexNow после деплоя.

## 3. Находки

Приоритеты: P0 — риск или прямая потеря трафика, P1 — заметный эффект при
небольших затратах, P2 — полезно, P3 — когда дойдут руки. «Кто»: код или
руками.

### 3.1 Формулировки (P0, закрыто 14 сентября)

| Находка | Что сделано |
|---|---|
| ~70 строк на 20 публичных страницах описывали, как открывается Discord, и обсуждали пинг через сторонние серверы | Переписано по правилу выше: Discord описывается только как «не открывается» или «доступ ограничен», аргумент про пинг сформулирован через Mute («трафик звонка идёт напрямую, игру не трогает») |
| Домашний FAQ содержал вопрос про Discord и пинг, он же уходил в FAQPage, на `/discord-alternative`, `/voice-chat` и в `llms-full.txt` | Заменён на «Влияет ли созвон в Mute на пинг в игре?» |
| Пост «Discord не работает: что делать» имел раздел с советами про сторонние сервисы и лишнее слово в заголовке | Заголовок сокращён, раздел заменён на «Что не поможет», адрес сохранён |
| Внешние копии (Teletype, карточка Product Radar) и внутренние документы содержали те же формулировки | Тексты в `docs/external/` и `docs/listing-copy.md` переписаны; на Teletype текст нужно заменить руками |
| Защиты от повторения не было | `scripts/check-content.mjs` в `prebuild`: запрещённые формулировки, даты из будущего, description длиннее 160 |

Юридические страницы (`/privacy`, `/terms`, `/delete-account`) не трогались:
там ссылки на приказ № 179 и право обжалования, это требования закона.

### 3.2 Техническое SEO

| Находка | Приоритет | Что делать | Кто |
|---|---|---|---|
| Google Search Console и Bing Webmaster не подключены: нет данных по запросам, нельзя запросить удаление `beta.mute.ac` из выдачи (она там всё ещё есть) | P0 | Токен GSC в `.env`, редеплой, sitemap; Bing импортирует сайт из GSC одной кнопкой | руками |
| Яндекс.Вебмастер: сайт подтверждён, но sitemap не отправлен, «Переобход» не использовался, регион не задан | P1 | Отправить sitemap, в «Переобход» отдать ~20 изменённых адресов, регион «Россия», проверить «Диагностику» и «Безопасность и нарушения» | руками |
| LCP гайдов на мобильном 4,8 с (`/games/steam`): иллюстрация над текстом грузится в полном размере | P1 | `sizes` и `priority` для первой картинки гайда, вариант поуже для мобильного, проверить Lighthouse после | код |
| Даты в frontmatter стояли на завтра (15.09), у одной статьи `updated` раньше `date`; sitemap отдавал lastmod из будущего | P1, сделано | Даты выровнены по дню деплоя, проверка в `check-content` | код |
| `lib/site.ts`: даты трёх посадочных отставали от правок | P1, сделано | Обновлены | код |
| 404 отдавался с title и description главной | P2, сделано | Свой metadata с `noindex` | код |
| `llms.txt` не был нигде объявлен | P2, сделано | Строка в robots.txt, `<link rel="alternate">` в layout, Cache-Control на robots и llms | код |
| Title длиннее 60 символов у 8 из 15 проверенных страниц (гайды и статьи 62–95 символов): выдача обрезает, CTR падает | P1 | Поле `seoTitle` в frontmatter с fallback на `title`, H1 остаётся длинным; сократить 8 заголовков до 60–65 | код |
| `/discord-alternative` и `/voice-chat` без `dateModified` в разметке, хотя даты в `lib/site.ts` есть | P2 | Узел WebPage с dateModified и og:modified_time | код |
| TechArticle релизов и инструкций: издатель вписан литералом, а не ссылкой `@id`, нет dateModified | P2 | Ссылка на `#organization`, `dateModified` из frontmatter | код |
| `softwareVersion` 0.2.2 (февраль), последние релизы без номера версии | P2 | Обновить из реального клиента или убрать поле | код |
| Нет RSS | P2 | `/blog/feed.xml` + `<link rel="alternate" type="application/rss+xml">` | код |
| Lighthouse «best practices» 77 на всех страницах | P2 | См. детали в разделе 4; чаще всего это сторонние куки счётчиков и ошибки в консоли, чинится точечно | код |
| robots.txt не называет AI-краулеров явно (разрешены через `*`) | P3 | Явные блоки Allow для GPTBot, OAI-SearchBot, ChatGPT-User, ClaudeBot, Claude-SearchBot, PerplexityBot, Google-Extended, Applebot-Extended, CCBot, YandexBot | код |
| Нет image-sitemap для иллюстраций гайдов | P3 | `<image:image>` в sitemap для `/games/*.webp` | код |
| nginx без brotli, HSTS без `preload` | P3 | При следующем обслуживании сервера | руками |
| `/releases/*` и `/install/*` без RelatedLinks и CTA (тупиковые страницы) | P2, сделано 14.09 | Подключены те же компоненты, что в блоге | код |
| Страницы со своим `openGraph` (обе посадочные, `/download`, все `[slug]`) теряли `og:site_name` и `og:locale`: Next подменяет объект из layout целиком | P1, сделано 14.09 | `OG_SITE` в `lib/site.ts`, разворачивается в каждый page-level `openGraph`; индексные и юридические страницы получили свой og | код |
| `/download` для краулеров с мобильным UA (Googlebot Smartphone, YandexMobileBot) заканчивался авторедиректом на `beta.mute.ac` с noindex | P1, сделано 14.09 | `OSProvider` держит для краулеров вариант «other»: без редиректов, обе ссылки на установщики в HTML | код |
| Еженедельные статус-посты двигали `date` и меняли `<title>` каждую неделю: `datePublished` плыл, Метрика делила страницу на несколько заголовков | P2, сделано 14.09 | `seoTitle` без даты, `date` фиксируется днём первой публикации, обновляется только `updated` (правило в README) | код |
| robots веб-приложения (`mute-webclient/public/robots.txt`) рекламировал `/register`, `/dashboard` и sitemap на несуществующем `app.mute.ac` | P2, сделано 14.09 | `Allow: /`, `Disallow: /api/`, `/uploads/`, без Sitemap; `public/sitemap.xml` удалён; обход оставлен, чтобы noindex был виден | код |

### 3.3 Страницы и сниппеты

| Находка | Приоритет | Что делать | Кто |
|---|---|---|---|
| Description длиннее 160 символов у 10 статей и гайдов и у `/delete-account` | P1, сделано | Сокращены, лимит проверяется перед сборкой | код |
| Гайды CS2 и Dota 2 не отдавали FAQPage: у CS2 раздел назывался иначе и был списком, у Dota 2 раздела не было | P1, сделано | Разделы «Коротко о частых вопросах» в формате `**Вопрос?** Ответ` | код |
| `/download`: 66 слов, ни одного H2 при приоритете 0.9 | P1 | Довести до ~300 слов: что внутри, требования, веб-версия, ссылки на инструкции SmartScreen и Gatekeeper, 3 коротких вопроса без разметки | код |
| Индексные страницы `/games`, `/blog`, `/releases` без H2 | P3 | Заголовки карточек как h2/h3 | код |
| `content/blog/mute-na-telefone.md` несёт `draftNote` с непроверенным абзацем про сворачивание браузера | P2 | Проверить на iPhone и Android, уточнить абзац, убрать заметку | руками |
| Одни и те же 6 ответов FAQ видны на трёх адресах (главная, `/discord-alternative`, `/voice-chat`) | P3 | Разметка уже не дублируется; при переписывании посадочных сделать тексты разными | код |

### 3.4 Контент

- Работает: 6 гайдов и 12 статей; гайд по Roblox стал второй страницей сайта,
  Steam — лучший реферер по конверсии, два статус-поста обновляются
  еженедельно. Темп сохранить.
- Слабое место: свежести много, а охвата по играм мало. Кандидаты на новые
  гайды по ПК-играм, спрос проверять подсказками Яндекса и Wordstat до
  написания: Rust, GTA 5 RP, Valorant, PUBG, Мир танков, Warface, Apex
  Legends, League of Legends, Deadlock, Marvel Rivals. Темп: два гайда в
  месяц.
- Кросс-игровые запросы без своей страницы: «созвон с другом с телефона,
  пока он играет на ПК», «голосовая комната на 8 человек бесплатно»,
  «созвониться вдвоём без установки».
- Статью про аналоги Discord обновлять раз в месяц: она в топе и цитируется.
- Правила остаются: без названий конкурентов, без MAX, честно про
  ограничения, каждый текст через humanizer, дата = день деплоя.

### 3.5 Видимость в нейросетях

| Находка | Приоритет | Что делать | Кто |
|---|---|---|---|
| `llms.txt`/`llms-full.txt` есть, факты и ограничения продукта описаны, английское резюме есть | ок | Обновлять вместе с сайтом (генерируются из данных) | код |
| Bing индексирует ~86 адресов, но без Bing Webmaster нет контроля; индекс Bing питает поиск ChatGPT | P0 | См. 3.2 | руками |
| Wikidata Q141046674 создан (алиасы, ОС, лицензия, дата запуска, Telegram), в документах числился как несделанный | P2 | Дополнить: платформа «web», логотип через Commons, версия, described at URL (Teletype, Product Radar); отметка в плейбуке поставлена | руками |
| AlternativeTo без карточки: nofollow, но самый цитируемый источник в ответах про альтернативы | P1 | Карточка по тексту из `docs/listing-copy.md` | руками |
| Журнал проверок в нейросетях не ведётся, хотя это заявленная метрика | P1 | Раз в месяц три вопроса в ChatGPT (с поиском), Алисе/Нейро, Perplexity; записывать в `docs/seo-positions.md` | руками |
| Английской страницы нет, при этом английские тексты для каталогов готовы | P3 | Одна страница `/en` с hreflang, когда дойдут руки до Product Hunt | код |

### 3.6 Внешние площадки

Из очереди `docs/seo-ugc-platforms.md` выполнена только статья на Teletype
(dofollow подтверждён). Перед любой новой публикацией тексты берутся из
репозитория после правок 14 сентября. Очередь: Product Radar (ассеты готовы),
AlternativeTo, три ответа на Ответах Mail, руководство в Steam Community,
Дзен с выключенными комментариями, TenChat, LiveJournal, Softportal и
soft.mydiv; решение по vc.ru и Песочнице Хабра; питчи в guidesgame,
lifehacker, hi-tech.mail.ru после двух-трёх внешних упоминаний.

Отдельно: на Teletype опубликована старая версия текста. Её нужно заменить
на текст из `docs/external/2026-09-teletype-golosovoy-chat-bez-vpn.md`.

### 3.7 Аналитика и измерение

- Цели Метрики (`open_web`, `download_click`, `download_win`, `download_mac`,
  `telegram_click`, `guide_cta`) шлются сайтом, но в интерфейсе Метрики их
  нужно завести как JavaScript-события, иначе они не попадают в отчёты.
  Сегмент «Лендинг» (Домен = mute.ac) обязателен: счётчик общий с
  веб-приложением.
- Таблица позиций заведена 14.09 в `docs/seo-positions.md`: восемь запросов
  аудита плюс четыре из плана по спросу; Яндекс (Москва, инкогнито),
  Google RU, Bing, раз в две недели. Первые числа снимает Alex.
- Полевых данных Web Vitals нет (мало трафика), Lighthouse снимать раз в
  месяц по двум страницам, см. раздел 4.

## 4. Замеры на 14 сентября 2026

- Ответы: `/` 200 за ~0,6 с, HTML 105 КБ; sitemap 41 адрес; `llms.txt`
  12 КБ, `llms-full.txt` 26 КБ; robots 200; снятые адреса отдают 308.
- Индексация: Bing `site:mute.ac` около 86 результатов; в Google (по данным
  веб-поиска из США) главная и `/blog/analogi-discord-v-rossii` в первой
  тройке по «аналог дискорда в россии без vpn», главная и
  `/discord-alternative` по «голосовой чат для игр без vpn»; по «discord не
  работает сегодня» сайта в топе нет, там детекторы сбоев и медиа.
- `beta.mute.ac` всё ещё встречается в выдаче Google и Bing при отдаваемом
  noindex (с 31 августа).
- Wikidata: Q141046674, метки en/ru, алиасы, P31, P856, P306, P407, P275,
  P571, P3789.
- Lighthouse (headless Chrome, live-сайт, 14.09.2026):

| Страница | Режим | Perf | A11y | Best practices | SEO | LCP | CLS | TBT |
|---|---|---|---|---|---|---|---|---|
| `/` | mobile | 97 | 92 | 77 | 100 | 2,5 с | 0,017 | 20 мс |
| `/` | desktop | 100 | 92 | 77 | 100 | 0,4 с | 0 | 0 |
| `/games/steam` | mobile | 79 | 92 | 77 | 100 | 4,8 с | 0 | 90 мс |
| `/games/steam` | desktop | 100 | 92 | 77 | 100 | 0,4 с | 0 | 0 |

Детали Lighthouse. «Best practices» 77 на всех страницах по одной причине:
сторонние куки счётчиков (Метрика и Top.Mail.Ru, 8 куки) и связанные записи
в inspector-issues. На ранжирование это не влияет; если понадобится сотня,
счётчики придётся грузить после согласия в cookie-баннере. Accessibility 92:
контраст вторичного текста (`text-text-secondary` в `body-text` и
`title-medium`, в гайдах ещё в блоке RelatedLinks) ниже 4,5:1, а ссылки в
тексте отличаются только цветом (`link-in-text-block`). Лечится чуть более
светлым вторичным цветом и подчёркиванием ссылок в тексте. Производительность
мобильной версии гайда: LCP 4,8 с на иллюстрации над текстом, Speed Index
4,3 с; у главной LCP 2,5 с, на границе «хорошо».

## 5. План

### Сделано в этом проходе (14 сентября)

Строгая чистка формулировок на 20 страницах и в документах; `check-content`
в `prebuild`; даты выровнены; 11 description сокращены; FAQPage у CS2 и Dota 2;
metadata у 404; `llms.txt` объявлен в robots и layout, Cache-Control у
служебных маршрутов; даты посадочных в `lib/site.ts`; документы приведены к
реальности (www работает, Wikidata есть, sitemap 41).

### Неделя 1, руками

1. Заменить текст статьи на Teletype; карточку Product Radar подавать по
   обновлённому тексту.
2. Google Search Console: токен в `.env`, редеплой, sitemap, Removals для
   `beta.mute.ac/*`. Bing Webmaster: импорт из GSC, sitemap, проверить
   IndexNow.
3. Яндекс.Вебмастер: sitemap, «Переобход» для изменённых адресов, регион,
   «Диагностика», «Безопасность и нарушения».
4. Метрика: завести шесть JS-целей и сегмент «Лендинг»; в Top.Mail.Ru цели
   `open_web`, `open_app`, `download`.
5. Создать `docs/seo-positions.md` с базовыми позициями и первым журналом
   проверок в нейросетях.
6. Проверить `mute-na-telefone` на iPhone и Android, убрать `draftNote`.

### Недели 1–2, код (сделано 14 сентября, второй проход)

- `seoTitle` в frontmatter, короткие title у 15 страниц; проверка длины в
  `check-content`.
- Мобильный LCP гайдов: первая иллюстрация через `next/image` с `priority`
  и `sizes`, `localPatterns` под `?v=N`.
- `/download` около 300 слов с пятью разделами и защитой от редиректа
  краулеров.
- Узел WebPage с dateModified на `/discord-alternative`, `/voice-chat`,
  `/download` и посадочных; CollectionPage с ItemList на `/games`, `/blog`,
  `/releases`; `@id` издателя и dateModified в TechArticle.
- RSS `/blog/feed.xml` (посты и гайды), ссылка в layout и в llms.
- Явные Allow для 13 AI-краулеров в robots.txt, Yandex-группа не тронута.
- RelatedLinks и CTA на `/releases/*` и `/install/*`; `related` во всех
  релизах и инструкциях.
- `/games` сгруппирован по `topic` («Не работает войс» / «Настройка») с
  блоком «Вашей игры нет в списке?».
- Посадочные `/voice-chat/phone`, `/voice-chat/screen-share`,
  `/voice-chat/rooms` из `content/landings/`; `/voice-chat` стал хабом.
- Параметр визита `site = landing` для сегмента Метрики.
- Image-расширение sitemap для иллюстраций гайдов (перенесено из «Месяцы 2–3»).

Осталось из этого блока: решение по `softwareVersion`, замечания Lighthouse
«best practices» и accessibility, `ym('hit')` на смену маршрута, если отчёты
по страницам покажут только точки входа.

### Месяц 1, внешние площадки

Product Radar, AlternativeTo, три ответа на Ответах Mail, руководство в
Steam Community, Дзен, TenChat, LiveJournal, Softportal и soft.mydiv;
решение по vc.ru и Хабру; дополнить Wikidata; отметки в плейбуке.

### Месяцы 2–3

Питчи в медиа после двух-трёх внешних упоминаний; страница `/en` с hreflang;
Product Hunt, startpack, Яндекс Бизнес, SaaSHub и Slant; страница «О проекте»
и подпись «Команда Mute» в статьях, юрлицо в подвале; image-sitemap;
ItemList на индексных страницах; brotli и HSTS preload; web-vitals в
параметры Метрики.

### Контент, постоянно

План по спросу с кластерами, календарём на двенадцать недель и
переименованиями вынесен в `seo-keyword-research-2026-09.md` (14 сентября,
по данным Метрики и подсказок). В постоянный ритм входят два статус-поста в
неделю (`date` первой публикации, `updated` = день деплоя, `seoTitle` без
даты); два гайда
по играм в месяц в порядке из раздела 2.1 H того документа (PUBG, Warface,
Valorant, Sea of Thieves, Genshin, Мир танков, Tarkov, War Thunder, Squad,
Phasmophobia, GTA 5 RP, Battlefield 6, Rust, Among Us, Standoff 2); две
сквозные статьи («Не слышно тиммейтов», «Голосовой чат на PS5 и Xbox»);
гайды «Как транслировать экран другу» и «Как позвонить другу в TeamSpeak»;
ежемесячное обновление статьи про аналоги Discord.

### Мониторинг и KPI

- Еженедельно: Вебмастер (диагностика, запросы), GSC после подключения.
- Раз в две недели: таблица позиций; `site:beta.mute.ac` до полного
  выпадения.
- Ежемесячно: проверка в нейросетях (цель: Mute называют все три системы на
  «какие есть аналоги дискорда в россии»), Lighthouse по двум страницам.
- KPI: показы и клики по восьми запросам; брендовые запросы («мьют», «мут
  голосовой чат»); цели `open_web` и `download_*` с органики; число внешних
  dofollow-упоминаний.
